# 复制这个文件为 .env，然后填真实值。.env 不要提交到 git，也不要发给任何AI。

# ========== 应用基础配置 ==========
# PYTHON_ENV 不由 pydantic-settings 读取，它通过 os.getenv 单独控制（默认 dev），不要加到 .env 里
APP_NAME=企业客服Agent

# ========== LLM 配置 ==========
LLM_API_KEY=your_key_here
LLM_BASE_URL=
LLM_MODEL_NAME=gpt-4o-mini
# LLM 调用超时（秒）和最大重试次数
LLM_TIMEOUT=60
LLM_MAX_RETRIES=2

# ========== Embedding 配置（V3 RAG）==========
EMBEDDING_MODEL_NAME=embedding-3
EMBEDDING_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
EMBEDDING_API_KEY=
# 输出维度：256/512/1024/2048，默认 1024
# EMBEDDING_DIMENSIONS=1024
# 相似度阈值：低于此值视为不相关，0 关闭过滤
# EMBEDDING_SIMILARITY_THRESHOLD=0.25
# 熔断器配置（V7）：连续 N 次失败后短路，等待 M 秒后恢复
EMBEDDING_FAILURE_THRESHOLD=3
EMBEDDING_RECOVERY_TIMEOUT=30

# ========== PostgreSQL 配置（V5 持久化）==========
# DATABASE_URL=postgresql+asyncpg://postgres:你的密码@localhost:5433/agent_demo
# 滑动窗口大小：每次请求最多携带最近 N 条历史消息
MAX_HISTORY_MESSAGES=10

# ========== JWT 认证（V7）==========
# ⚠️  生产环境必须使用强随机密钥（至少 32 位）
# 生成方式：python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-secret-key-must-be-at-least-32-characters-long
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# ========== 服务器配置 ==========
HOST=0.0.0.0
PORT=8000

# ========== 日志配置 ==========
# LOG_LEVEL=info    # debug / info / warning / error
